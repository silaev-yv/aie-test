# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры генерации отчёта

- Максимум гистограмм: **6**
- Top-K категорий: **5**
- Порог проблемных пропусков: **30.0%**

## Качество данных (эвристики)

- Оценка качества: **0.59**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Есть константные колонки: **False**
- Есть категориальные с высокой кардинальностью: **False**
- Есть колонки с большим количеством нулей: **True**
  - Много нулей: churned
- Есть подозрительные дубликаты ID: **True**
  - Подозрительные ID: user_id

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Top-5 значений для категориальных признаков.
См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

Отображено до 6 числовых колонок.
См. файлы `hist_*.png`.
